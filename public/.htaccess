<IfModule mod_rewrite.c>
    RewriteEngine On
    # No RewriteBase: target is relative to this directory, so works at doc root and subpath (e.g. /webhooks/public)
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [L,QSA]
</IfModule>
